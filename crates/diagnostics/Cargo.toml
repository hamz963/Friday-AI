[package]
name = "nova-diagnostics"
version = "0.1.0"
edition = "2021"

[dependencies]
nova-core = { path = "../core" }
nova-terminal = { path = "../terminal" }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
regex = { workspace = true }
